services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: batchdb
      POSTGRES_USER: batch
      POSTGRES_PASSWORD: batch
    ports: [ "5432:5432" ]
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
