spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/batchdb
    username: batch
    password: batch
  jpa:
    hibernate:
      ddl-auto: update
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false
